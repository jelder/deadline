{
  "name": "deadline",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "date-fns": "^2.0.0-alpha.10",
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "react-scripts build && aws s3 sync --acl=public-read build/ s3://deadlines.jacobelder.com"
  }
}
